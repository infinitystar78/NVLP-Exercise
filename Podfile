# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NVLP' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NVLP
    pod 'SwiftKeychainWrapper'
    pod 'SwiftyJSON', '~> 4.0'
    pod 'Alamofire', '~> 5.2'
    pod "KRProgressHUD"
    pod 'Charts'
  
  target 'NVLPTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NVLPUITests' do
    # Pods for testing
  end

end
